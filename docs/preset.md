Here are some preset settings of infra we have already done.

### GCP Infrastructure Setup

**Project & CLI**
```
Project ID: chc-snowflake-agent
Region:     us-central1
```

**Secrets Created in Secret Manager**
```
ANTHROPIC_API_KEY
OPENAI_API_KEY
SNOWFLAKE_ACCOUNT
SNOWFLAKE_USER
SNOWFLAKE_PASSWORD
SNOWFLAKE_DATABASE
SNOWFLAKE_WAREHOUSE
SNOWFLAKE_SCHEMA
JWT_SECRET
USERS
```

**Artifact Registry**
```
Repository: chc-snowflake-agent
Format:     Docker
Location:   us-central1
```

**Service Account**
```
Name:   chc-snowflake-agent-sa
Email:  chc-snowflake-agent-sa@chc-snowflake-agent.iam.gserviceaccount.com

Roles:
- roles/secretmanager.secretAccessor   ← Secret Manager access
- roles/datastore.user                 ← Firestore access
- roles/artifactregistry.writer        ← Push Docker images (CI/CD)
- roles/run.admin                      ← Deploy Cloud Run (CI/CD)
```


---

### GitHub Repository Setup

**Repository**
```
Name:       chc-snowflake-agent
Visibility: Private
Shared with: sfc-gh-setli, sfc-gh-nwiegand, sfc-gh-wenli
```

**GitHub Actions Secrets**
```
GCP_PROJECT_ID           chc-snowflake-agent
GCP_SA_KEY               Service Account JSON key
FIREBASE_SERVICE_ACCOUNT Firebase deploy key (auto-generated by Firebase CLI)
```
---

### Firebase Hosting Setup

```
Project:        chc-snowflake-agent
Public dir:     dist
Single-page:    Yes
GitHub CI/CD:   Enabled
```

**Firestore Data Structure**

```
firestore/
└── users/
    └── {username}/
        └── sessions/
            └── {session_id}/
                ├── created_at: timestamp
                └── messages/
                    ├── msg_001: { role: "user", content: "...", timestamp }
                    └── msg_002: { role: "assistant", content: "...", timestamp }
```